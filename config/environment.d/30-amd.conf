AMD_VULKAN_ICD=RADV
# ACO is already RADV's default on current Mesa. Keeping RADV_PERFTEST unset is
# safer for Steam/Proton than forcing old perf-test flags globally.
# RADV_PERFTEST=aco
MESA_SHADER_CACHE_DIR=/home/sairu/.cache/mesa_shader_cache
MESA_SHADER_CACHE_MAX_SIZE=4G
LIBVA_DRIVER_NAME=radeonsi
VDPAU_DRIVER=radeonsi
