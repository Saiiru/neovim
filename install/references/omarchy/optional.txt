# Optional Omarchy-inspired packages. Install only by explicit group.
docker                      # Optional dev infra; enable service only if chosen.
docker-buildx               # Docker builder.
docker-compose              # Compose workflow.
cups                        # Printing stack.
cups-browsed                # Printer discovery; review security/network.
cups-filters                # Printing.
cups-pdf                    # PDF printer.
avahi                       # mDNS; useful but network-exposed.
nss-mdns                    # mDNS resolver integration.
ufw                         # Firewall frontend; choose ufw OR firewalld, not both.
# ufw-docker                # Not in pacman here; Docker+UFW integration needs manual/AUR review.
tesseract                   # OCR.
tesseract-data-eng          # OCR English data.
obs-studio                  # Recording/streaming.
gpu-screen-recorder         # GPU recording; optional, check AMD behavior.
xournalpp                   # PDF annotation.
# localsend                 # Not in pacman here; use Flatpak/AUR if desired.
