# JaKooLit Arch-Hyprland items rejected for Arcane Niri defaults.
# This group is audit-only and blocked by the installer.

hyprland                     # Reject: Niri is canonical compositor.
hypridle                     # Reject: use Niri-compatible idle path.
hyprlock                     # Reject: lock path must be Niri-compatible.
xdg-desktop-portal-hyprland  # Reject: compositor-specific portal conflicts with Niri.
hyprshot                     # Reject: screenshot flow uses grim/slurp/satty or wl-copy.
swww                         # Reject/default: current wallpaper flow uses repo scripts.
wlogout                      # Reject: local power menu script is canonical.
wallust                      # Reject/default: theme sync should be explicit in this repo.
nvidia-dkms                  # Reject on current AMD machine; install via gpu-nvidia-* only when detected/chosen.
nvidia-utils                 # Reject on current AMD machine; install via gpu-nvidia-* only when detected/chosen.
