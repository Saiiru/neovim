# Pending Removal

The following files or directories are candidates for removal but are kept temporarily for verification.

- `config/hypr`: Might be removed if Niri remains the sole focus.
- `config/waybar`: Might be removed if KORA shell fully replaces it.
- `install/package_lists/macos`: Likely legacy as this is a Linux workstation focus.
- `docs/*.md` (orphaned): Any documentation that doesn't fit the new category system.
